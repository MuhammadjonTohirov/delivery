from django.urls import path, include
from rest_framework.routers import DefaultRouter

app_name = 'promotions'

# Basic URL structure for promotions
urlpatterns = [
    # Promotion endpoints would be implemented here
    # Keeping minimal for now
]
